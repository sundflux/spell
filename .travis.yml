language: ruby

rvm:
  - 2.4

addons:
  apt:
    packages:
      - docker-ce

services:
  - docker


install:
  - gem install pygmy
  - pygmy up

script:
  - composer create-project druidfi/spell:dev-$TRAVIS_BRANCH mysite --no-interaction
  - cd mysite
  - composer build-dev
